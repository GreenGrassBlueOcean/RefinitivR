% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rd_esg.R
\name{rd_GetESG}
\alias{rd_GetESG}
\title{Retrieve ESG Data from LSEG}
\usage{
rd_GetESG(
  RDObject = rd_connection(),
  universe,
  view = c("scores-full", "scores-standard", "measures-full", "measures-standard",
    "basic", "universe"),
  start = NULL,
  end = NULL,
  use_field_names_in_headers = TRUE,
  raw_output = FALSE,
  debug = FALSE,
  cache = NULL
)
}
\arguments{
\item{RDObject}{A connection object returned by \code{\link{RefinitivJsonConnect}()}.
Defaults to \code{RefinitivJsonConnect()} if not supplied.}

\item{universe}{Character vector of instrument codes (RICs, PermIDs, etc.).}

\item{view}{Character string selecting the ESG view. One of:
\describe{
  \item{\code{"scores-full"}}{Complete ESG scores with all pillars and
    categories (default).}
  \item{\code{"scores-standard"}}{Standard ESG scores (subset of full).}
  \item{\code{"measures-full"}}{Detailed ESG measures across all pillars.}
  \item{\code{"measures-standard"}}{Standard ESG measures (subset of full).}
  \item{\code{"basic"}}{Basic ESG overview data.}
  \item{\code{"universe"}}{List of instruments covered by the ESG universe.}
}}

\item{start}{Optional integer. Initial value of the financial year range
(e.g. \code{2020}).}

\item{end}{Optional integer. End value of the financial year range
(e.g. \code{2023}).}

\item{use_field_names_in_headers}{Logical. If \code{TRUE} (default), column
names come from the API's \code{title} field; if \code{FALSE}, from the
internal \code{name} field.}

\item{raw_output}{Logical. If \code{TRUE}, returns the raw parsed JSON list
instead of a \code{data.frame}.}

\item{debug}{Logical. If \code{TRUE}, prints debug messages.}

\item{cache}{Controls caching. \code{NULL} (default) defers to
\code{getOption("refinitiv_cache", FALSE)}. \code{TRUE} uses the
function default TTL (300 s). \code{FALSE} disables caching. A positive
numeric value sets the cache TTL in seconds. See \code{\link{rd_ClearCache}}.}
}
\value{
A \code{data.frame} with ESG data, or the raw JSON list if
  \code{raw_output = TRUE}.
}
\description{
Fetches Environmental, Social, and Governance (ESG) data from the LSEG
Data Platform. Supports multiple views including scores, measures, basic
overview, and the ESG universe listing.
}
\examples{
\dontrun{
# Full ESG scores for Apple and Microsoft
rd_GetESG(universe = c("AAPL.O", "MSFT.O"))

# Standard measures for a specific year range
rd_GetESG(
  universe = "AAPL.O", view = "measures-standard",
  start = 2020, end = 2023
)

# Basic overview
rd_GetESG(universe = "IBM.N", view = "basic")
}

}
