# Inspect the credential vault safely

Returns a summary object that prints key names without exposing values.

## Usage

``` r
refinitiv_vault()
```

## Value

An object of class `"refinitiv_vault"`.
