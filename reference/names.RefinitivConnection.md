# List methods on a RefinitivConnection object

Returns only the user-facing API method names, filtering out any
environment internals.

## Usage

``` r
# S3 method for class 'RefinitivConnection'
names(x)
```

## Arguments

- x:

  A `RefinitivConnection` object.

## Value

Character vector of method names.
