# Print a RefinitivConnection object

Displays connection metadata (base URL, port, credential status) without
revealing API keys or tokens.

## Usage

``` r
# S3 method for class 'RefinitivConnection'
print(x, ...)
```

## Arguments

- x:

  A `RefinitivConnection` object.

- ...:

  Ignored.

## Value

`x` (invisibly).
